<%= form_for([@project, @project.tasks.build]) do |f| %>
	<%= f.text_field :content, placeholder: 'New Task' %> 	
	<hr>
	Gravidade:
    <%= f.select :gravity, options_for_select(['sem_gravidade', 'pouco_grave', 'grave', 'muito_grave', 'extremamente_grave']) %>
	<hr>
	<%= f.submit %> 	
<% end %> 